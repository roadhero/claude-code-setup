# OS / editor cruft
.DS_Store
Thumbs.db
*.swp
.idea/
.vscode/

# Never commit secrets or local env, even into a config repo
*.env
.env.*
!.env.example
*.pem
*.key
credentials.json
service-account*.json

# Local settings stash created by the web/compute swap (see STRUCTURE.md)
settings.web.json
