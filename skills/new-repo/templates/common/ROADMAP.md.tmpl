# Roadmap

> The plan. Each shipped item gets a "(shipped vX.Y.Z)" annotation so the
> `docs-reconciler` can reconcile it against the CHANGELOG (§10).

| Milestone | Theme | Status |
|-----------|-------|--------|
| M0 | TODO | ⏳ |
