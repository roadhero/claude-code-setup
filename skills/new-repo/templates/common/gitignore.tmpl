# --- Secrets (CLAUDE.md §11 — never commit) ---
*.env
.env.*
!.env.example
*.pem
*.key
*.keystore
*.jks
*.p12
**/secrets/
credentials.json
service-account*.json
*.tfstate
*.tfstate.*

# --- OS / editor ---
.DS_Store
.idea/
.vscode/
*.swp

# --- Build / deps (extend per stack) ---
