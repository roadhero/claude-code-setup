# Single source of truth for the app version. Bump BEFORE tagging.
# build.gradle.kts and release.yml read these; inlining elsewhere is drift.
# versionCode = MAJOR*10000 + MINOR*100 + PATCH.  versionName must equal the git tag (vX.Y.Z).
versionCode=100
versionName=0.1.0
